# Mitra ML fine-tuning pipeline
# For Colab T4: install with  !pip install -r requirements.txt
# bitsandbytes wheels are CUDA-specific; Colab already has a compatible version.

anthropic>=0.40.0
python-dotenv>=1.0.0

# Core training stack (versions tested on Colab T4 as of 2025-06)
transformers>=4.45.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.43.1
accelerate>=0.34.0
datasets>=2.19.0

# Inference (used by distill_intent.py in the backend)
torch>=2.2.0          # Colab ships this; install separately for local CPU use

# Utilities
scikit-learn>=1.4.0
pandas>=2.2.0
tqdm>=4.66.0
