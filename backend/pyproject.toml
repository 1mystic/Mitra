[project]
name = "mitra-backend"
version = "1.0.0"
description = "Mitra Career Intelligence OS — FastAPI backend"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"
select = ["E", "F", "I"]

[tool.ruff.isort]
known-first-party = ["app"]
